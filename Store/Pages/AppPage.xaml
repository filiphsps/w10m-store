<Page
    x:Class="Store.Pages.AppPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Store.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="ListViewItemStretchStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>
    </Page.Resources>

    <Pivot x:Name="PrimaryPivot" Title="AppName">
        <PivotItem Header="Overview">
            <ScrollViewer ScrollViewer.VerticalScrollMode="Enabled">
                <StackPanel>
                    <Grid Margin="0,10,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="125"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <Border x:Name="AppImgWrapper" Grid.Column="0" Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                            <Image x:Name="AppImg" Stretch="Uniform" Height="130" Width="130" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Stretch">
                            <TextBlock x:Name="AppNameStr" Style="{ThemeResource SubtitleTextBlockStyle}">Name</TextBlock>
                            <TextBlock x:Name="AppAuthorStr" Style="{ThemeResource CaptionTextBlockStyle}" Tapped="AppAuthorStr_Tapped">Author</TextBlock>
                            <Button x:Name="InstallBtn" HorizontalAlignment="Stretch" Margin="0,15,0,0" Click="InstallBtn_Click">Install</Button>
                        </StackPanel>
                    </Grid>
                    <TextBlock x:Name="AppDescStr" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}">AppDescription</TextBlock>
                </StackPanel>
            </ScrollViewer>
        </PivotItem>
        <PivotItem Header="Details">
            <ScrollViewer ScrollViewer.VerticalScrollMode="Enabled">
                <StackPanel>
                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Information</TextBlock>
                    <TextBlock x:Name="AppVersion">Version: <Run x:Name="AppVersionStr" />.</TextBlock>
                    <TextBlock>Namespace: <Run x:Name="AppNsStr" />.</TextBlock>
                    <TextBlock x:Name="AppTimestamp">Last updated: <Run x:Name="AppTimestampStr" />.</TextBlock>
                    <TextBlock>Size: <Run x:Name="AppSizeStr" /> MB.</TextBlock>

                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,15,0,0">Contributors</TextBlock>
                    <ListView x:Name="ContributorsList" SelectionMode="None" ItemContainerStyle="{StaticResource ListViewItemStretchStyle}" ScrollViewer.VerticalScrollMode="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}"/>
                                    <TextBlock Text="{Binding Role}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,15,0,0">Dependencies</TextBlock>
                    <ListView x:Name="DependencyList" SelectionMode="None" ItemContainerStyle="{StaticResource ListViewItemStretchStyle}" ScrollViewer.VerticalScrollMode="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Namespace}" Style="{ThemeResource CaptionTextBlockStyle}" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding Version}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </PivotItem>
    </Pivot>

    <Page.BottomAppBar>
        <CommandBar></CommandBar>
    </Page.BottomAppBar>
</Page>
