<Page
    x:Class="Store.Pages.AppPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Store.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Pivot x:Name="PrimaryPivot" Title="AppName">
        <PivotItem Header="Overview">
            <ScrollViewer>
                <StackPanel>
                    <Grid Margin="0,10,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.45*"/>
                            <ColumnDefinition Width="0.55*"/>
                        </Grid.ColumnDefinitions>
                        <Image x:Name="AppImg" Grid.Column="0" Source="https://via.placeholder.com/250" Margin="0,0,15,0" />
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="AppNameStr" Style="{ThemeResource SubtitleTextBlockStyle}">Name</TextBlock>
                            <TextBlock x:Name="AppAuthorStr" Style="{ThemeResource CaptionTextBlockStyle}">Author</TextBlock>
                            <TextBlock x:Name="AppVersionStr" Style="{ThemeResource CaptionTextBlockStyle}">Version</TextBlock>
                            <Button x:Name="InstallBtn" HorizontalAlignment="Stretch" Margin="0,15,0,-4" Click="InstallBtn_Click">Install</Button>
                            <ProgressBar x:Name="Progress" Value="0" Background="Transparent" />
                        </StackPanel>
                    </Grid>
                    <TextBlock x:Name="AppDescStr" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}">AppDescription</TextBlock>
                </StackPanel>
            </ScrollViewer>
        </PivotItem>
        <PivotItem Header="More info">
            <StackPanel>
                <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Dependencies</TextBlock>
            </StackPanel>
        </PivotItem>
    </Pivot>
</Page>
